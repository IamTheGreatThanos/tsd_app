import 'package:flutter/material.dart';

class MoveDataScreen extends StatelessWidget {
  const MoveDataScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
